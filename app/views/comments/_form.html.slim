.comment-form.row
  .comment-form__header
    = image_tag current_user.image_url, class: 'comment-form__header__img', size: '32x32'
    .comment-form__header__label
      |  コメントを投稿する 
  = form_for(@article.comments.build(user: current_user), remote: true) do |f|
    = f.hidden_field :user_id
    = f.hidden_field :article_id
    .form-group
      = f.text_area :text, placeholder: 'コメントを入力してください', class: 'form-control'
    .actions.pull-right
      = f.submit '投稿する', class: 'btn btn-primary'
